---
phase: 03-core-skills-working-memory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - MEMORY.md
  - .claude/memory/projects.md
  - .claude/memory/preferences.md
  - CLAUDE.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "MEMORY.md exists at vault root with structured summary of active projects, priorities, recent changes, and user preferences"
    - "Topic files exist in .claude/memory/ for detailed per-area context (projects.md, preferences.md)"
    - "CLAUDE.md documents the four-layer memory architecture with session and working memory fully specified"
    - "CLAUDE.md documents layers 3-4 (long-term summary, project-specific) as stubs with clear extension points for Phase 4"
    - "Claude can read MEMORY.md and topic files at session start to restore context without user re-explaining"
  artifacts:
    - path: "MEMORY.md"
      provides: "Working memory summary: active projects, priorities, recent changes, user preferences, links to topic files"
      min_lines: 20
    - path: ".claude/memory/projects.md"
      provides: "Detailed per-project context: status, decisions, blockers, next actions"
      min_lines: 10
    - path: ".claude/memory/preferences.md"
      provides: "User patterns and preferences observed during sessions"
      min_lines: 10
    - path: "CLAUDE.md"
      provides: "Updated memory architecture section documenting all 4 layers with session startup behavior"
      contains: "Memory Architecture"
  key_links:
    - from: "MEMORY.md"
      to: ".claude/memory/projects.md"
      via: "wiki-link reference in Topic Files section"
      pattern: "\\[\\[.claude/memory/projects.md\\]\\]"
    - from: "MEMORY.md"
      to: ".claude/memory/preferences.md"
      via: "wiki-link reference in Topic Files section"
      pattern: "\\[\\[.claude/memory/preferences.md\\]\\]"
    - from: "CLAUDE.md"
      to: "MEMORY.md"
      via: "Memory Architecture section references MEMORY.md as working memory root"
      pattern: "MEMORY\\.md"
---

<objective>
Build the working memory system -- MEMORY.md at vault root, topic files in .claude/memory/, and update CLAUDE.md with the four-layer memory architecture.

Purpose: Claude needs persistent context across sessions. When the user opens a new Claude Code session, Claude should already know the active projects, recent changes, priorities, and user preferences without being told. This plan implements layers 1-2 (session + working memory) fully and documents layers 3-4 (long-term summary + project-specific) as stubs for Phase 4.

Output: MEMORY.md at vault root, two starter topic files in .claude/memory/, and an updated Memory Architecture section in CLAUDE.md.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-skills-working-memory/03-RESEARCH.md
@.planning/phases/03-core-skills-working-memory/03-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MEMORY.md and topic files</name>
  <files>
    MEMORY.md
    .claude/memory/projects.md
    .claude/memory/preferences.md
  </files>
  <action>
**Create `MEMORY.md` at vault root** with this structure:

```yaml
---
updated: 2026-03-07
---
```

```markdown
# Working Memory

Claude's persistent context across sessions. Updated after significant actions.

## Active Projects

- (No active user projects yet -- this section will be populated as the user creates projects)

## Current Priorities

1. Vault setup and AI-native skill development (Phase 3 in progress)

## Recent Changes

- Working memory system initialized (2026-03-07)
- Scanning infrastructure complete: /scan skill, vault-index, link-map, tag-index (2026-03-07)
- English vault foundation complete: all templates, MOCs, guides translated (2026-03-07)

## User Preferences

- (Preferences will be learned during sessions -- writing style, tag habits, workflow patterns)

## Topic Files

Detailed per-area context lives in `.claude/memory/`:
- [[.claude/memory/projects.md]] -- per-project state, decisions, blockers
- [[.claude/memory/preferences.md]] -- user patterns and preferences
```

MEMORY.md should be concise (under 50 lines per user decision from CONTEXT.md). It is a summary hub that links to topic files for details.

**Create `.claude/memory/projects.md`:**

```yaml
---
updated: 2026-03-07
---
```

```markdown
# Project Context

Detailed per-project state, decisions, and context. Updated when projects change status.

## Firstbrain Development

- **Status:** In progress (Phase 3: Core Skills & Working Memory)
- **Phases complete:** 1 (Foundation), 2 (Scanning Engine)
- **Current focus:** Building /create, /daily, /connect, /health skills
- **Key decisions:** Zero external dependencies, hand-rolled parsers, Claude-interpreted SKILL.md pattern
- **Next:** Phase 4 (Deep Memory & Semantic Search)

---

*New projects will be added here as the user creates them via /create or naturally.*
```

**Create `.claude/memory/preferences.md`:**

```yaml
---
updated: 2026-03-07
---
```

```markdown
# User Preferences

Patterns and preferences observed during sessions. Updated when Claude notices recurring behaviors.

## Confirmed Patterns

- (None yet -- preferences will emerge during actual vault usage)

## Communication Style

- (To be learned -- does user prefer brief or detailed responses?)

## Workflow Habits

- (To be learned -- morning review routine? Weekly review day? Tagging patterns?)

---

*Preferences are written here when Claude observes confirmed patterns (not after every interaction).*
*Per memory architecture: writes triggered by significant actions only.*
```

Per user decision: Memory writes triggered by significant actions (project status changes, new projects, confirmed patterns), NOT after every note edit.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); console.assert(fs.existsSync('MEMORY.md'), 'MEMORY.md missing'); console.assert(fs.existsSync('.claude/memory/projects.md'), 'projects.md missing'); console.assert(fs.existsSync('.claude/memory/preferences.md'), 'preferences.md missing'); const mem = fs.readFileSync('MEMORY.md', 'utf8'); console.assert(mem.includes('Working Memory')); console.assert(mem.includes('Active Projects')); console.assert(mem.includes('.claude/memory/projects.md')); console.assert(mem.includes('.claude/memory/preferences.md')); const lines = mem.split('\n').length; console.assert(lines <= 50, 'MEMORY.md exceeds 50 lines: ' + lines); console.log('Memory files: all assertions passed');"</automated>
  </verify>
  <done>MEMORY.md exists at vault root under 50 lines with structured sections (Active Projects, Priorities, Recent Changes, Preferences, Topic Files). Topic files exist in .claude/memory/ with starter structure for projects and preferences. All files have frontmatter with updated date.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md memory architecture section</name>
  <files>CLAUDE.md</files>
  <action>
Update the existing Memory Architecture section in CLAUDE.md to document the full four-layer system with operational instructions.

**Find the existing section** (currently reads):
```markdown
## Memory Architecture

Claude operates with a layered memory model:

1. **Session memory** -- Within a single conversation (automatic, no setup needed)
2. **Working memory** -- `MEMORY.md` + `.claude/memory/` topic files (persists across sessions)
3. **Long-term summary memory** -- Distilled patterns, recurring themes, insights (Phase 4)
4. **Project-specific memory** -- Per-project state, decisions, and context (Phase 4)

> Layers 3-4 are planned. Currently using layers 1-2.
```

**Replace with expanded version** that adds operational instructions for Claude:

```markdown
## Memory Architecture

Claude operates with a layered memory model:

### Layer 1: Session Memory (automatic)
Within a single conversation. No setup needed -- Claude naturally tracks discussion context.

### Layer 2: Working Memory (active)
Persists across sessions via files Claude reads and writes:

- **`MEMORY.md`** (vault root) -- Concise summary: active projects, priorities, recent changes, preferences. Under 50 lines.
- **`.claude/memory/projects.md`** -- Detailed per-project context: status, decisions, blockers, next actions.
- **`.claude/memory/preferences.md`** -- User patterns and preferences confirmed through repeated observation.

**Session startup:** Silently read `MEMORY.md` and vault-index.json. Know the context without announcing it.

**Memory writes:** Triggered by significant actions only:
- Project status changes (new, completed, blocked)
- New user projects created
- User preferences confirmed through repeated behavior
- NOT after every note edit or routine action

**MEMORY.md maintenance:** Rewrite (not append) on each update. Keep under 50 lines. Detailed context goes in topic files.

### Layer 3: Long-term Summary Memory (Phase 4 stub)
*Not yet implemented.* Will distill recurring themes, organizational patterns, and cross-session insights. Extension point: `.claude/memory/insights.md`.

### Layer 4: Project-specific Memory (Phase 4 stub)
*Not yet implemented.* Will track per-project state, decisions, and blockers with richer context than the current projects.md. Extension point: `.claude/memory/project-{name}.md` files.
```

**Important:** Only modify the Memory Architecture section. Do NOT change any other section of CLAUDE.md. Read the full file first, find the exact section boundaries, and replace only that section.

The updated section should:
1. Keep the 4-layer structure
2. Add operational instructions for session startup (silent load)
3. Add memory write triggers (significant actions only)
4. Add MEMORY.md maintenance rules (rewrite, not append; under 50 lines)
5. Document layers 3-4 as stubs with extension point file paths
6. Remove the "> Layers 3-4 are planned" note (replaced by inline stub descriptions)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const claude = fs.readFileSync('CLAUDE.md', 'utf8'); console.assert(claude.includes('Layer 1: Session Memory')); console.assert(claude.includes('Layer 2: Working Memory')); console.assert(claude.includes('Layer 3: Long-term Summary Memory')); console.assert(claude.includes('Layer 4: Project-specific Memory')); console.assert(claude.includes('Session startup')); console.assert(claude.includes('Memory writes')); console.assert(claude.includes('Phase 4 stub')); console.assert(!claude.includes('Layers 3-4 are planned')); console.log('CLAUDE.md memory section: all assertions passed');"</automated>
  </verify>
  <done>CLAUDE.md Memory Architecture section expanded with operational instructions (silent startup load, significant-action-only writes, 50-line MEMORY.md limit). Layers 1-2 fully documented with file paths and behaviors. Layers 3-4 documented as Phase 4 stubs with extension point paths. No other CLAUDE.md sections modified.</done>
</task>

</tasks>

<verification>
1. MEMORY.md exists at vault root, under 50 lines, with frontmatter
2. .claude/memory/projects.md exists with project context section
3. .claude/memory/preferences.md exists with preferences section
4. MEMORY.md links to both topic files via wiki-links
5. CLAUDE.md Memory Architecture section contains all 4 layers with operational instructions
6. CLAUDE.md Memory Architecture section has Phase 4 stub markers for layers 3-4
7. No other sections of CLAUDE.md were modified
</verification>

<success_criteria>
- MEMORY.md is concise (under 50 lines), structured, and links to topic files
- Topic files exist with starter structure and frontmatter
- CLAUDE.md documents the complete four-layer memory architecture
- Operational instructions specify: silent session startup, significant-action-only writes, rewrite-not-append
- Layers 3-4 have clear extension points for Phase 4
- All files use consistent formatting and frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-skills-working-memory/03-03-SUMMARY.md`
</output>
