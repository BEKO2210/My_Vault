---
phase: 01-foundation-vault-preparation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: []
autonomous: false
requirements: [FOUND-07]

must_haves:
  truths:
    - "Zero German-language fragments remain in any vault markdown file (outside .planning/)"
    - "Zero broken wiki-links exist (every [[target]] resolves to an existing file or is a valid click-to-create placeholder)"
    - "Dataview queries in Home.md and all MOCs return results (not 'No results')"
    - "Obsidian graph view shows a connected graph (no disconnected nodes from broken links)"
    - "Obsidian Properties panel displays frontmatter correctly for note files"
    - "Obsidian search finds notes by English content"
  artifacts: []
  key_links:
    - from: "Home.md"
      to: "06 - Atlas/MOCs/*.md"
      via: "Wiki-links from dashboard to all MOCs"
      pattern: "\\[\\[.*MOC\\]\\]"
    - from: "06 - Atlas/MOCs/*.md"
      to: "Home.md"
      via: "MOCs link back to Home"
      pattern: "\\[\\[Home\\]\\]"
---

<objective>
Run automated integrity checks on the entire vault and then have the user verify Obsidian compatibility by opening the vault in Obsidian.

Purpose: The English rewrite and governance setup are complete. This plan ensures nothing was broken in the process -- no stale German references, no broken wiki-links, no Dataview query failures, and full Obsidian UI compatibility.
Output: Verified vault integrity. Phase 1 complete.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-vault-preparation/01-01-SUMMARY.md
@.planning/phases/01-foundation-vault-preparation/01-02-SUMMARY.md
@.planning/phases/01-foundation-vault-preparation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated vault integrity checks</name>
  <files>(no files modified -- read-only verification)</files>
  <action>
Run a comprehensive set of automated checks to verify vault integrity after the English rewrite.

**Check 1: No German fragments remain**
```bash
# Search for common German words that should have been translated
grep -rPn "[äöüÄÖÜß]|Verbindungen|Beschreibung|Aufgaben|Erstelle|Notizen|Uebersicht|Arbeitsweise|Konventionen|Kernideen|Erklaerung|Begruendung|Konsequenzen|Naechste Schritte|Rueckblick|Reflexion|Neue Eintraege|Verwandte|Neues Projekt|Neuer Bereich|Neue Ressource|Neues Tool|Neuer Zettel|Neue Entscheidung|Neue Person|Neues Meeting|Neues Snippet" --include="*.md" . | grep -v ".planning/" | grep -v ".claude/"
```
Expected: 0 results. If any German fragments found, list them for review.

**Check 2: Wiki-link integrity**
```bash
# Extract all wiki-link targets from vault files
grep -rPoh "\[\[([^\]|]+)" --include="*.md" . | grep -v ".planning/" | grep -v ".claude/" | sed 's/\[\[//' | sort -u > /tmp/link-targets.txt

# List all .md filenames (without extension and path)
find . -name "*.md" -not -path "./.planning/*" -not -path "./.claude/*" | sed 's|.*/||;s|\.md$||' | sort -u > /tmp/file-names.txt

# Find link targets that don't match any file (potential broken links)
# Note: Some targets are "click to create" placeholders -- these are acceptable
comm -23 /tmp/link-targets.txt /tmp/file-names.txt
```
Review output: known click-to-create targets (New Project, New Area, New Resource, etc.) are acceptable. Unknown broken targets are problems.

**Check 3: Dataview query integrity**
```bash
# List all Dataview query blocks and their FROM/WHERE clauses
grep -rn "FROM\|WHERE\|TABLE\|LIST" --include="*.md" . | grep -v ".planning/" | grep -v ".claude/" | grep -i "dataview\|FROM\|WHERE"
```
Verify: FROM clauses reference existing folder paths. WHERE clauses use valid frontmatter field names.

**Check 4: Template variable preservation**
```bash
# Verify template variables still present in template files
grep -rn "{{date}}\|{{title}}\|{{time}}" "05 - Templates/"
```
Expected: At least one match per template file (all 12 templates should have {{date}} at minimum).

**Check 5: Frontmatter validity**
```bash
# Check all vault files have valid frontmatter (start with ---)
for f in $(find . -name "*.md" -not -path "./.planning/*" -not -path "./.claude/*"); do
  head -1 "$f" | grep -q "^---" || echo "MISSING FRONTMATTER: $f"
done
```
Expected: Only system docs without frontmatter are acceptable (README.md, CONTRIBUTING.md may not have it).

**Check 6: CLAUDE.md line count**
```bash
wc -l CLAUDE.md
```
Expected: Between 120-200 lines.

**Check 7: Rules directory complete**
```bash
ls -la .claude/rules/
cat .claude/changelog.md | head -5
```
Expected: 5 rule files, changelog exists with header.

If any check fails, report the specific failures clearly so they can be fixed before the human verification checkpoint.
  </action>
  <verify>
    <automated>cd "C:/Users/belki/Desktop/Firstbrain-main" && echo "German check:" && grep -rPc "[äöüÄÖÜß]|Verbindungen|Beschreibung|Aufgaben" --include="*.md" . 2>/dev/null | grep -v ".planning/" | grep -v ".claude/" | grep -v ":0$" | wc -l && echo "Template vars:" && grep -rlc "{{date}}" "05 - Templates/" | wc -l && echo "Rules:" && ls .claude/rules/ | wc -l</automated>
    Expected: German check = 0 files with matches, Template vars = 12 (all templates), Rules = 5 files
  </verify>
  <done>All 7 automated checks pass: no German fragments, wiki-links valid, Dataview queries intact, template variables preserved, frontmatter present, CLAUDE.md correct length, rules directory complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Obsidian compatibility</name>
  <files>(no files modified -- human verification only)</files>
  <action>
Present the following verification checklist to the user. The user must open Obsidian and verify each item.

What was built: The entire vault has been rewritten to English (all templates, MOCs, system docs, file names) and AI governance infrastructure has been created (CLAUDE.md, .claude/rules/, changelog). This checkpoint verifies everything works correctly in Obsidian.

Verification steps for the user:

1. **Graph View** (Ctrl+G or click graph icon):
   - Graph displays without errors
   - Notes are connected (not isolated orphan dots from broken links)
   - Click on a few nodes to verify they navigate correctly

2. **Search** (Ctrl+Shift+F):
   - Search for "Project" -- should find Home.md, Projects MOC, Project template
   - Search for "Connections" -- should find it in templates (was "Verbindungen")
   - Search for a German word like "Verbindungen" -- should find NO results in vault files

3. **Properties Panel** (click on any note, check right sidebar):
   - Open a template file (e.g., 05 - Templates/Project.md)
   - Frontmatter properties (type, created, tags) display correctly
   - No YAML parsing errors

4. **Dataview Queries**:
   - Open Home.md -- check if Dataview blocks render (may show "No results" if no actual project notes exist, but should NOT show errors)
   - Open Projects MOC.md -- Dataview query should render without errors
   - Column headers should be in English (e.g., "Modified" not "Geaendert")

5. **Template Insertion**:
   - Try creating a test note using Ctrl+Shift+T (Templates core plugin)
   - Select any template -- it should insert with English content
   - Delete the test note after verifying

6. **Navigation**:
   - Open Home.md -- all wiki-links should be clickable
   - Click through to a MOC -- MOC content should be in English
   - All "create new" links (e.g., [[New Project]]) should work as click-to-create

Resume signal: Type "approved" if everything works, or "issues: [description]" if problems found.
  </action>
  <verify>User confirms all 6 verification areas pass by typing "approved".</verify>
  <done>User has verified: graph view works, search works, Properties panel works, Dataview queries render, templates insert correctly, navigation functions. Obsidian compatibility confirmed.</done>
</task>

</tasks>

<verification>
1. All 7 automated integrity checks pass (Task 1)
2. User confirms Obsidian graph view, search, Properties panel, and Dataview queries work (Task 2)
3. No German fragments found anywhere in the vault
4. Wiki-links resolve correctly
5. Templates insert properly
</verification>

<success_criteria>
- Automated checks confirm: zero German fragments, valid wiki-links, intact Dataview queries, preserved template variables, complete rules directory
- User confirms: Obsidian graph view works, search works, Properties panel works, Dataview queries render, templates insert correctly, navigation functions
- Phase 1 is fully complete: all 7 FOUND requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vault-preparation/01-04-SUMMARY.md`
</output>
