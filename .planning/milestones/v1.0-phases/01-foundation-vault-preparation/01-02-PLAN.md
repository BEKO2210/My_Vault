---
phase: 01-foundation-vault-preparation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "CLAUDE.md"
  - ".claude/rules/naming.md"
  - ".claude/rules/linking.md"
  - ".claude/rules/frontmatter.md"
  - ".claude/rules/governance.md"
  - ".claude/rules/templates.md"
  - ".claude/changelog.md"
  - ".claude/settings.json"
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06]

must_haves:
  truths:
    - "CLAUDE.md is ~150 lines, written in English, covering identity, vault structure, memory architecture, governance, and quick reference"
    - "Five .claude/rules/ files exist covering naming, linking, frontmatter, governance, and templates conventions"
    - "The governance rule file defines three zones (content, structure, system) with explicit AUTO/PROPOSE/NEVER classification"
    - "The NEVER list is explicitly enumerated: no file deletion, no note merging, no body content edits without request, no renames without approval"
    - ".claude/changelog.md exists with a header and format example ready for logging"
    - "Path-specific rules use YAML frontmatter paths: field for scoping (e.g., templates.md scoped to 05 - Templates/**)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AI-native core instructions (~150 lines)"
      contains: "Identity, Vault Structure, Memory Architecture, Governance, Quick Reference"
    - path: ".claude/rules/naming.md"
      provides: "File naming conventions for all folders"
    - path: ".claude/rules/linking.md"
      provides: "Wiki-link rules and minimum connection requirements"
    - path: ".claude/rules/frontmatter.md"
      provides: "YAML frontmatter standards for all note types"
    - path: ".claude/rules/governance.md"
      provides: "AUTO/PROPOSE/NEVER classification with three-zone model"
    - path: ".claude/rules/templates.md"
      provides: "Template usage rules (path-scoped to 05 - Templates/**)"
    - path: ".claude/changelog.md"
      provides: "Changelog infrastructure for logging autonomous actions"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/*.md"
      via: "Pointer to rules directory for detailed conventions"
      pattern: "\\.claude/rules/"
    - from: ".claude/rules/governance.md"
      to: "CLAUDE.md"
      via: "Governance rules referenced in CLAUDE.md"
      pattern: "AUTO|PROPOSE|NEVER"
---

<objective>
Create the AI-native governance infrastructure: rewrite CLAUDE.md as a concise core document (~150 lines), create per-concern rule files in .claude/rules/, establish the three-zone governance model with AUTO/PROPOSE/NEVER classification, and set up changelog infrastructure.

Purpose: This governance system defines how Claude operates in the vault -- what it can do autonomously, what requires approval, and what is forbidden. It is the trust foundation for all future AI features.
Output: CLAUDE.md (~150 lines), 5 rule files in .claude/rules/, changelog.md initialized.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-vault-preparation/01-CONTEXT.md
@.planning/phases/01-foundation-vault-preparation/01-RESEARCH.md

Key user decisions (LOCKED -- non-negotiable):

**CLAUDE.md structure:** Layered -- concise core rules (~150 lines) in CLAUDE.md, detailed per-concern docs in .claude/rules/
**Personality:** Proactive partner -- suggests connections, flags opportunities, shares insights. Engaged but not chatty.
**Memory architecture:** Document the 4-layer model (session, working, long-term, project) in CLAUDE.md from Phase 1 even though implementation comes later.
**Rules organization:** Per concern (naming.md, linking.md, frontmatter.md, governance.md, templates.md) -- each covers all folders for that topic.

**Governance zones:**
- Content zone (notes, daily entries, personal knowledge): Light assist -- auto-fix broken links and fill missing frontmatter, NEVER edit body text
- Structure zone (folders, MOCs, navigation, templates): AUTO for safe additions (adding note to existing MOC), PROPOSE for risky changes (new folders, restructuring)
- System zone (.claude/, CLAUDE.md, rules, config): Self-maintaining -- AUTO-update memory files and changelog. Config and rules changes are PROPOSE

**NEVER zone (hard boundaries):**
- Never delete any file
- Never merge notes
- Never change note body content without explicit request
- Never rename files without explicit approval

**Changelog:** Log significant actions only (structural changes, new files, moved files, MOC updates). Skip trivial metadata fills.

**Claude's discretion:** Exact changelog entry format, validation check structure, CLAUDE.md section ordering, specific rule file wording.

**CLAUDE.md recommended sections:**
```
# Firstbrain -- AI-Native Second Brain
## Identity & Role
## Vault Structure
## Memory Architecture
## Governance
## Quick Reference
## Rules
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md as AI-native core document</name>
  <files>CLAUDE.md</files>
  <action>
Completely rewrite CLAUDE.md from scratch. The current file is ~117 lines of German instructions. The new file must be ~150 lines of English, serving as Claude's core operating manual for this vault.

**Structure (recommended by user, exact ordering at Claude's discretion):**

```markdown
# Firstbrain -- AI-Native Second Brain

## Identity & Role
- Who Claude is: proactive knowledge partner, not passive assistant
- Personality: suggests connections, flags opportunities, shares insights. Engaged but not chatty
- Core value: user focuses on thinking and creating, Claude handles filing and organizing

## Vault Structure
- Folder tree with one-line purpose per folder (same 8 folders as current CLAUDE.md)
- Entry points: Home.md, START HERE.md, Workflow Guide.md, Tag Conventions.md

## Memory Architecture
- Document the 4-layer model even though implementation comes in Phase 3/4:
  1. Session memory (within conversation, automatic)
  2. Working memory (MEMORY.md + .claude/memory/ topic files, persists across sessions)
  3. Long-term summary memory (distilled patterns, recurring themes -- Phase 4)
  4. Project-specific memory (per-project state, decisions, context -- Phase 4)
- Note: "Layers 3-4 are planned. Currently using layers 1-2."

## Governance
- Three zones: content, structure, system (one line each with key rule)
- Action classification: AUTO, PROPOSE, NEVER (concise definitions)
- NEVER list (4 items, bolded)
- Pointer: "See .claude/rules/governance.md for detailed classification"

## Quick Reference
- Template table (12 templates with target folder and type)
- Template variables: {{date}}, {{title}}, {{time}}
- Frontmatter minimum: type, created, tags
- Wiki-link rules: always use [[]], minimum 2 connections per new note

## Rules
- Pointer to .claude/rules/ directory
- List: naming.md, linking.md, frontmatter.md, governance.md, templates.md
- Note: "Rules without paths: load always. Rules with paths: load when working in matching directories."
```

**Constraints:**
- Target ~150 lines (hard max: 200 lines). Every line must pass: "Would removing this cause Claude to make mistakes?"
- All English, professional but warm tone
- Do NOT duplicate detailed rules that belong in .claude/rules/ files
- Do NOT include the old German content
- Template table must list all 12 templates with correct target folders and type values
- Naming convention examples must use English names (e.g., "Decision -- Topic.md" not "Entscheidung -- Thema.md")
  </action>
  <verify>
    <automated>cd "C:/Users/belki/Desktop/Firstbrain-main" && wc -l CLAUDE.md && echo "---" && grep -c "Identity\|Vault Structure\|Memory Architecture\|Governance\|Quick Reference\|Rules" CLAUDE.md && echo "---" && grep -cP "[äöüÄÖÜß]" CLAUDE.md</automated>
    Expected: ~150 lines, 6+ section headers found, 0 German characters
  </verify>
  <done>CLAUDE.md is ~150 lines of English AI-native instructions. Contains all required sections: Identity & Role, Vault Structure, Memory Architecture, Governance, Quick Reference, Rules. No German text. All 12 templates listed. NEVER list present. Memory architecture documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create .claude/rules/ files and changelog infrastructure</name>
  <files>
    .claude/rules/naming.md
    .claude/rules/linking.md
    .claude/rules/frontmatter.md
    .claude/rules/governance.md
    .claude/rules/templates.md
    .claude/changelog.md
    .claude/settings.json
  </files>
  <action>
Create 5 rule files in .claude/rules/ (create the directory if needed) and initialize the changelog. Also update .claude/settings.json to remove the old German instructions field (if present) since CLAUDE.md now serves that purpose.

**File 1: .claude/rules/naming.md** (NO paths: frontmatter -- loads always)
```
File naming conventions for all vault folders:
- Projects: Descriptive name -> "Website Relaunch.md"
- Areas: Area name -> "Health.md", "Finances.md"
- Resources: Prefix + name -> "Book -- Title.md", "Tool -- Name.md", "Zettel -- Idea.md"
- Daily Notes: Date -> "YYYY-MM-DD.md"
- Decisions: "Decision -- Topic.md"
- Meetings: "Meeting -- Topic YYYY-MM-DD.md"
- People: "Person -- Name.md"
- Code Snippets: "Snippet -- Name.md"
- All filenames in English
- Use " -- " (space-dash-dash-space) as separator, not em-dash
```

**File 2: .claude/rules/linking.md** (NO paths: frontmatter -- loads always)
```
Wiki-link conventions:
- Always use [[Note Name]] for cross-references
- Use [[Note Name|Display Name]] when display text differs from filename
- Every new note MUST have at least 2 wiki-links to existing notes
- Every new note MUST have a ## Connections section with categorized links
- When creating a note that fits a MOC topic, add it to that MOC
- When fixing broken links: search for [[old name]] and replace with [[correct name]]
- Link categories: Related, Source, Supports, Contradicts, Introduced By, Previous
```

**File 3: .claude/rules/frontmatter.md** (NO paths: frontmatter -- loads always)
```
YAML frontmatter standards:
- Every note MUST have: type, created, tags
- type values: project, area, resource, tool, person, meeting, decision, code-snippet, zettel, daily, review
- created: YYYY-MM-DD format
- tags: array, always English, always lowercase
- Status values: active, planned, on-hold, completed, archived
- Priority values: high, medium, low
- Optional fields by type:
  - project: status, priority, area, due
  - area: status
  - resource/tool/zettel: status
  - meeting: date, participants
  - decision: status, related-project
  - daily: (minimal -- type, created, tags)
  - review: period (weekly/monthly)
```

**File 4: .claude/rules/governance.md** (NO paths: frontmatter -- loads always)
```
Evolution governance -- three zones with action classification.

## Zones

### Content Zone (notes body text, daily entries, personal knowledge)
- AUTO: Fix broken wiki-links, fill missing frontmatter fields
- PROPOSE: Nothing else in content
- NEVER: Edit body text without explicit request

### Structure Zone (folders, MOCs, navigation, templates)
- AUTO: Add note to existing MOC, fix broken links in MOCs
- PROPOSE: Create new folders, restructure existing folders, modify templates, create new MOCs
- NEVER: Delete folders, remove notes from MOCs without request

### System Zone (.claude/, CLAUDE.md, rules, config)
- AUTO: Update memory files (.claude/memory/), append to changelog
- PROPOSE: Modify CLAUDE.md, modify rule files, change settings
- NEVER: Delete system files

## Action Classification

**AUTO** -- Execute immediately. Log to .claude/changelog.md.
Actions: File to correct folder, fix broken links, fill missing frontmatter, add to MOC, update memory.

**PROPOSE** -- Present to user with rationale. Wait for approval.
Actions: New folders, restructuring, template changes, new MOCs, config changes, rename suggestions.

**NEVER** -- Do not execute under any circumstances.
- Never delete any file
- Never merge notes
- Never change note body content without explicit request
- Never rename files without explicit approval

## Pre-Action Validation
Before any autonomous action:
1. Identify which zone the action affects
2. Check if the action is AUTO, PROPOSE, or NEVER
3. If AUTO: execute and log to changelog
4. If PROPOSE: present to user, wait for approval
5. If NEVER: stop immediately

## Changelog
Log significant actions only: structural changes, new files, moved files, MOC updates.
Skip trivial: metadata fills, memory updates.
Format: see .claude/changelog.md
```

**File 5: .claude/rules/templates.md** (WITH paths: frontmatter)
```yaml
---
paths:
  - "05 - Templates/**"
---
```
```
Template conventions:
- Templates are READ-ONLY. Never modify template files directly.
- Template variables: {{date}} (YYYY-MM-DD), {{title}} (filename), {{time}} (HH:mm)
- Every template must have in frontmatter: type, created (set to {{date}}), tags
- Every template must end with a ## Connections section containing wiki-link placeholders
- When creating a note from a template:
  1. Copy template content to new file in correct folder
  2. Replace all {{date}}, {{title}}, {{time}} with actual values
  3. Fill in at least 2 wiki-links in the Connections section
  4. Add any relevant tags to frontmatter
```

**File 6: .claude/changelog.md**
```markdown
# Changelog

All significant autonomous actions by Claude are logged here.

## Format

- **AUTO** | Action description | Affected file(s)
- **PROPOSE** | Proposal description | Status: pending/approved/rejected

---

(Entries will be added below this line, grouped by date)
```

**File 7: .claude/settings.json**
Read the existing file first. If it contains a German `instructions` field, remove it (CLAUDE.md now serves this purpose). Keep all other settings (permissions, etc.) unchanged. If the file has `allowedTools` or other permission settings, preserve them exactly.
  </action>
  <verify>
    <automated>cd "C:/Users/belki/Desktop/Firstbrain-main" && ls -la .claude/rules/ && echo "---" && ls -la .claude/changelog.md && echo "---" && head -1 .claude/rules/templates.md && echo "---" && grep -c "NEVER\|AUTO\|PROPOSE" .claude/rules/governance.md</automated>
    Expected: 5 rule files listed, changelog exists, templates.md starts with "---" (YAML frontmatter), governance.md has 10+ classification references
  </verify>
  <done>Five rule files created in .claude/rules/ (naming.md, linking.md, frontmatter.md, governance.md, templates.md). templates.md has path-specific scoping. governance.md defines three zones with full AUTO/PROPOSE/NEVER classification. .claude/changelog.md initialized. settings.json cleaned up.</done>
</task>

</tasks>

<verification>
1. `wc -l CLAUDE.md` is between 120-200 lines
2. `ls .claude/rules/` shows: naming.md, linking.md, frontmatter.md, governance.md, templates.md
3. `.claude/changelog.md` exists with header and format
4. `grep "paths:" .claude/rules/templates.md` returns a match (path-scoped)
5. `grep -c "paths:" .claude/rules/naming.md .claude/rules/linking.md .claude/rules/frontmatter.md .claude/rules/governance.md` all return 0 (not path-scoped, load always)
6. CLAUDE.md references .claude/rules/ directory
7. No German text in any created file
</verification>

<success_criteria>
- CLAUDE.md is ~150 lines of English AI-native instructions with all required sections
- 5 rule files exist in .claude/rules/ covering naming, linking, frontmatter, governance, templates
- templates.md is path-scoped to "05 - Templates/**"
- governance.md defines complete three-zone model with AUTO/PROPOSE/NEVER
- NEVER list has exactly 4 items: no delete, no merge, no body edit, no rename
- Changelog initialized with header and format documentation
- No German text in any new or modified file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vault-preparation/01-02-SUMMARY.md`
</output>
