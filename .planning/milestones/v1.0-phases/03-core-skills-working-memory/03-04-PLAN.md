---
phase: 03-core-skills-working-memory
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [NOTE-01, NOTE-02, NOTE-03, NOTE-04, CONN-01, CONN-02, CONN-03, CONN-04, MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "All 8 .cjs modules load without errors (create-utils, daily-utils, connect-utils, health-utils)"
    - "All 4 SKILL.md files have valid frontmatter and reference their utility modules"
    - "Template-to-folder mapping is complete for all 12 types"
    - "Date arithmetic handles month and year boundary crossings"
    - "Rollover extraction correctly identifies unchecked checkboxes and skips already-rolled items"
    - "Connection scoring produces ranked results with evidence"
    - "Health analysis detects orphans and broken links from current vault-index and link-map"
    - "MEMORY.md and topic files exist with valid structure"
    - "CLAUDE.md memory section contains all 4 layers with operational instructions"
    - "All created notes render correctly in Obsidian with working wiki-links and frontmatter"
  artifacts:
    - path: ".agents/skills/create/SKILL.md"
      provides: "/create skill definition"
    - path: ".agents/skills/create/create-utils.cjs"
      provides: "Template utilities"
    - path: ".agents/skills/daily/SKILL.md"
      provides: "/daily skill definition"
    - path: ".agents/skills/daily/daily-utils.cjs"
      provides: "Daily note utilities"
    - path: ".agents/skills/connect/SKILL.md"
      provides: "/connect skill definition"
    - path: ".agents/skills/connect/connect-utils.cjs"
      provides: "Connection utilities"
    - path: ".agents/skills/health/SKILL.md"
      provides: "/health skill definition"
    - path: ".agents/skills/health/health-utils.cjs"
      provides: "Health utilities"
    - path: "MEMORY.md"
      provides: "Working memory root"
    - path: ".claude/memory/projects.md"
      provides: "Project context"
    - path: ".claude/memory/preferences.md"
      provides: "User preferences"
  key_links:
    - from: ".agents/skills/create/create-utils.cjs"
      to: ".agents/skills/scan/scanner.cjs"
      via: "require for index freshness"
      pattern: "require.*scanner"
    - from: ".agents/skills/daily/daily-utils.cjs"
      to: ".agents/skills/create/create-utils.cjs"
      via: "require for getDateVars and substituteVariables re-export"
      pattern: "require.*create-utils"
    - from: ".agents/skills/connect/connect-utils.cjs"
      to: ".agents/skills/scan/utils.cjs"
      via: "require for loadJson"
      pattern: "require.*utils"
    - from: ".agents/skills/health/health-utils.cjs"
      to: ".agents/skills/scan/utils.cjs"
      via: "require for loadJson"
      pattern: "require.*utils"
---

<objective>
Verify all Phase 3 artifacts work correctly -- automated tests for all utility modules and SKILL.md files, plus a human checkpoint for Obsidian compatibility.

Purpose: Ensure the four skills (/create, /daily, /connect, /health) and working memory system are complete and functional before moving to Phase 4. Catch integration issues between modules and verify vault renders correctly in Obsidian.

Output: Verification results confirming all Phase 3 requirements are met.
</objective>

<execution_context>
@C:/Users/belki/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/belki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-skills-working-memory/03-01-SUMMARY.md
@.planning/phases/03-core-skills-working-memory/03-02-SUMMARY.md
@.planning/phases/03-core-skills-working-memory/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 3 artifacts</name>
  <files></files>
  <action>
Run a comprehensive verification script that tests all Phase 3 artifacts. No files are modified -- this is read-only verification.

**1. Module Loading Tests:**
```javascript
// Verify all 4 utility modules load without errors
const createUtils = require('./.agents/skills/create/create-utils.cjs');
const dailyUtils = require('./.agents/skills/daily/daily-utils.cjs');
const connectUtils = require('./.agents/skills/connect/connect-utils.cjs');
const healthUtils = require('./.agents/skills/health/health-utils.cjs');
```

**2. Create-Utils Tests:**
- `getTemplateInfo` returns correct mapping for all 12 types
- `buildFileName('tool', 'Docker', '2026-03-07')` === `'Tool -- Docker.md'`
- `buildFileName('daily', '', '2026-03-07')` === `'2026-03-07.md'`
- `buildFileName('meeting', 'Standup', '2026-03-07')` === `'Meeting -- Standup 2026-03-07.md'`
- `buildFileName('code-snippet', 'React Hook', '2026-03-07')` === `'Snippet -- React Hook.md'`
- `getDateVars('2026-01-01').yesterday` === `'2025-12-31'` (year boundary)
- `getDateVars('2026-03-01').yesterday` === `'2026-02-28'` (month boundary)
- `getDateVars('2026-03-07').tomorrow` === `'2026-03-08'` (normal case)
- `substituteVariables` replaces all {{variable}} patterns with no leftovers
- `substituteVariables` handles `{{date:MMMM YYYY}}` -> "March 2026"
- `substituteVariables` handles `{{date:ww}}` -> correct ISO week number

**3. Daily-Utils Tests:**
- `formatRolloverSection` with items produces `## Rolled Over` section
- `formatRolloverSection` with empty array returns empty string
- `mergeRolloverItems` with existing rollover section adds only new items
- `mergeRolloverItems` with no rollover section creates one
- `getDateVars` re-export works (same function as create-utils)

**4. Connect-Utils Tests:**
- `formatConnectionSuggestions` produces numbered list with confidence and evidence
- `findConnections` with mock data returns scored, sorted results
- Templates and MOCs are filtered out of results

**5. Health-Utils Tests:**
- `levenshtein('docker', 'docekr')` <= 2
- `levenshtein('same', 'same')` === 0
- `levenshtein('', 'abc')` === 3
- `classifyFix([{name: 'Docker', distance: 1}])` returns action='auto'
- `classifyFix([{name: 'A', distance: 1}, {name: 'B', distance: 1}])` returns action='propose'
- `classifyFix([])` returns action='manual'
- `suggestFixes('Docekr', ['Docker', 'Docker Compose', 'Git'])` includes 'Docker'

**6. SKILL.md Verification:**
- All 4 SKILL.md files exist and have valid frontmatter (name, trigger, description, version)
- /scan SKILL.md (Phase 2) still exists and is unchanged
- /create SKILL.md references create-utils.cjs functions
- /daily SKILL.md references daily-utils.cjs functions
- /connect SKILL.md references connect-utils.cjs functions
- /health SKILL.md references health-utils.cjs functions

**7. Memory System Verification:**
- MEMORY.md exists at vault root, under 50 lines, has frontmatter
- .claude/memory/projects.md exists with frontmatter
- .claude/memory/preferences.md exists with frontmatter
- MEMORY.md links to both topic files
- CLAUDE.md contains "Layer 1", "Layer 2", "Layer 3", "Layer 4"
- CLAUDE.md contains "Phase 4 stub" markers
- CLAUDE.md contains "Session startup" and "Memory writes" instructions

**8. Integration Tests (using actual vault data):**
- Run `scan('.')` to ensure indexes are fresh
- Load vault-index.json, link-map.json, tag-index.json
- Call `findConnections` with a real note path from the vault
- Call `analyzeHealth` with real vault-index and link-map
- Verify orphan count and broken link count are non-negative numbers

Print a summary: "X/Y checks passed" with details for any failures.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=['create/create-utils.cjs','daily/daily-utils.cjs','connect/connect-utils.cjs','health/health-utils.cjs','create/SKILL.md','daily/SKILL.md','connect/SKILL.md','health/SKILL.md']; let pass=0; for(const c of checks){if(fs.existsSync('.agents/skills/'+c)){pass++}else{console.log('MISSING: '+c)}} console.assert(pass===8, 'Only '+pass+'/8 skill files exist'); console.assert(fs.existsSync('MEMORY.md')); console.assert(fs.existsSync('.claude/memory/projects.md')); console.assert(fs.existsSync('.claude/memory/preferences.md')); console.log('All 11 Phase 3 artifacts exist');"</automated>
  </verify>
  <done>All 8 utility module and SKILL.md files load without errors. All function exports verified. Template mapping covers all 12 types. Date arithmetic handles boundaries. Rollover deduplication works. Connection scoring produces ranked results. Health analysis detects orphans and broken links. Memory files exist with valid structure. CLAUDE.md updated with 4-layer architecture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Obsidian compatibility verification</name>
  <files></files>
  <action>
Human verifies that all Phase 3 artifacts render correctly in Obsidian.

**What was built:** Phase 3 skills infrastructure (/create, /daily, /connect, /health utility modules and SKILL.md definitions), working memory system (MEMORY.md, topic files, CLAUDE.md memory architecture update).

**How to verify:**
1. Open the vault in Obsidian
2. MEMORY.md check: Open MEMORY.md -- verify it renders with proper headings and links to topic files. Click the topic file links to verify they navigate correctly.
3. Graph view: Open graph view -- verify MEMORY.md appears connected. Verify no new broken links introduced.
4. Properties panel: Open MEMORY.md in Properties view -- verify `updated` frontmatter field displays correctly.
5. Search: Search for "Working Memory" -- verify MEMORY.md appears in results.
6. File explorer: Verify `.claude/memory/` folder contains projects.md and preferences.md. Verify `.agents/skills/` contains create/, daily/, connect/, health/ subdirectories alongside scan/.
7. Template check: Open a template (e.g., `05 - Templates/Tool.md`) -- verify templates are unchanged (READ-ONLY rule respected).
8. CLAUDE.md check: Open CLAUDE.md and scroll to Memory Architecture -- verify the 4-layer section renders cleanly with Layer 1-4 headings.

**Resume signal:** Type "approved" if everything renders correctly, or describe any issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification in Obsidian"</automated>
  </verify>
  <done>User confirms all Phase 3 artifacts render correctly in Obsidian: MEMORY.md links work, graph view shows connections, Properties panel displays frontmatter, templates are unchanged, CLAUDE.md memory section renders cleanly.</done>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. All automated checks pass (Task 1)
2. Obsidian renders all new files correctly (Task 2)
3. No templates were modified (READ-ONLY rule)
4. No existing vault notes were modified (NEVER zone respected)
5. All 11 Phase 3 requirements (NOTE-01..04, CONN-01..04, MEM-01..03) have corresponding artifacts
</verification>

<success_criteria>
- All 8 skill files (4 SKILL.md + 4 .cjs) load and pass automated tests
- All 3 memory files exist with valid structure
- CLAUDE.md memory section updated with operational instructions
- Obsidian compatibility confirmed by human verification
- Zero templates modified
- Zero existing vault notes modified
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-skills-working-memory/03-04-SUMMARY.md`
</output>
